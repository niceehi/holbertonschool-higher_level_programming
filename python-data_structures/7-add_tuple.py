#!/usr/bin/python3

def add_tuple(tuple_a=(), tuple_b=()):
    if len(tuple_a[0]) > 0:
        a1 = tuple_a[0]
    else:
        a1 = 0
    if len(tuple_a[1]) > 0:
        a2 = tuple_a[1]
    else:
        a2 = 0
    if len(tuple_b[0]) > 0:
        a3 = tuple_b[0]
    else:
        a3 = 0
    if len(tuple_b[1]) > 0:
        a4 = tuple_b[1]
    else:
        a4 = 0
